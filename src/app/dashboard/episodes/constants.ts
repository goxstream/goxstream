import type {
  EpisodeItem,
  EpisodeStats,
  ServerNode,
  SubtitleCoverageItem,
} from "./types";

export const MOCK_EPISODES: EpisodeItem[] = [];

export const MOCK_EPISODE_STATS: EpisodeStats = {
  totalEpisodes: 0,
  publishedEpisodes: 0,
  draftEpisodes: 0,
  serverIssues: 0,
  totalViews: 0,
};

export const MOCK_SERVER_NODES: ServerNode[] = [];

export const MOCK_SUBTITLE_COVERAGE: SubtitleCoverageItem[] = [];
